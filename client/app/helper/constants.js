/* global XLSX, XLS */
define([], function () {

  const SUM = 'SUM';
  const AVG = 'AVG';
  const STD = 'STD';
  const LIN = 'LIN';

  const SELF = 'SELF';


  return {
    SUM,
    AVG,
    STD,
    LIN,
    SELF
  };

});
